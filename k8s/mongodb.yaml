---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "mongodb"
  namespace: "default"
  labels:
    app: "mongodb"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "mongodb"
  template:
    metadata:
      labels:
        app: "mongodb"
    spec:
      containers:
      - name: "mongo-1"
        image: "mongo:latest"
---
apiVersion: "autoscaling/v2"
kind: "HorizontalPodAutoscaler"
metadata:
  name: "mongodb-hpa-hn2q"
  namespace: "default"
  labels:
    app: "mongodb"
spec:
  scaleTargetRef:
    kind: "Deployment"
    name: "mongodb"
    apiVersion: "apps/v1"
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: "Resource"
    resource:
      name: "cpu"
      target:
        type: "Utilization"
        averageUtilization: 80
---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "mongodb-service"
  namespace: "default"
  labels:
    app: "mongodb"
spec:
  ports:
  - protocol: "TCP"
    port: 27017
    targetPort: 27017
  selector:
    app: "mongodb"
  type: "ClusterIP"
