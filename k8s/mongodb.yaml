---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "deployment-mongo"
  namespace: "default"
  labels:
    app: "deployment-mongo"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "deployment-mongo"
  template:
    metadata:
      labels:
        app: "deployment-mongo"
    spec:
      containers:
      - name: "mongo-1"
        image: "mongo:latest"
---
apiVersion: "autoscaling/v2"
kind: "HorizontalPodAutoscaler"
metadata:
  name: "deployment-mongo-hpa-wyaw"
  namespace: "default"
  labels:
    app: "deployment-mongo"
spec:
  scaleTargetRef:
    kind: "Deployment"
    name: "deployment-mongo"
    apiVersion: "apps/v1"
  minReplicas: 1
  maxReplicas: 1
  metrics:
  - type: "Resource"
    resource:
      name: "cpu"
      target:
        type: "Utilization"
        averageUtilization: 80
---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "deployment-mongo-service"
  namespace: "default"
  labels:
    app: "deployment-mongo"
spec:
  ports:
  - protocol: "TCP"
    port: 27017
    targetPort: 27017
  selector:
    app: "deployment-mongo"
  type: "ClusterIP"
